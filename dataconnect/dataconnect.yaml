specVersion: "v1beta"
serviceId: "emotion"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "emotion-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
